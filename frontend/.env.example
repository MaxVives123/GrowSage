# Backend URL — set to your Railway backend service URL in production
NEXT_PUBLIC_API_URL=https://your-backend.up.railway.app

# "true" → demo mode (no API needed), "false" → real RAG API
NEXT_PUBLIC_DEMO_MODE=false
